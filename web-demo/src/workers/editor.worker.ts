/**
 * Monaco Editor Worker Entry Point
 *
 * This file serves as the entry point for Monaco's editor web worker.
 * Vite will bundle this with the ?worker suffix to create a proper worker bundle.
 */

// Import and initialize Monaco's editor worker
import 'monaco-editor/editor/editor.worker.js'
