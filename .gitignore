# Rust
/target/
**/*.rs.bk
*.pdb
# Cargo.lock IS committed — this workspace ships binaries (CLI/server/l10n/fuzz)
# and the release flow treats it as a version source. Do not ignore it.

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
*.bak*
.DS_Store

# Claude Code
.claude/settings.local.json

# Testing
*.profdata
*.profraw
lcov.info
conformance_test_output.log
validation_run.log
*.dSYM/
tf_*.tcl
test*.db
testdb
# WAL-mode durability siblings left behind by TCL test runs (#5843):
# <root>.wal, <root>.lock, <root>-checkpoints/ for test db files
test*.wal
test*.lock
test*-checkpoints/
out.db
out.wal
out.lock
out-checkpoints/
# Generic WAL siblings for any .vbsql database opened ad hoc (name-scoped
# rules above only cover test*/out* prefixes; no tracked file matches these)
*.wal
*-checkpoints/
*.db.tmp*
.*.tmp*

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
venv/
ENV/
.pytest_cache/
.coverage
htmlcov/

# Node.js
node_modules/
*.tsbuildinfo
coverage/
logs/
# Lock files - only pnpm-lock.yaml should be committed for packages/ (workspace root has no lockfile of its own)
package-lock.json
yarn.lock

# Benchmark results
benchmark_results.json
!web-demo/public/benchmarks/benchmark_results.json
conformance-report.html
benchmarks.db
benchmarks/suite/results/

# Test results database backups
# test_results/ - now tracked to preserve 100% conformance milestone

# Fuzzing
fuzz/target/
fuzz/artifacts/
# fuzz/Cargo.lock stays ignored — the fuzz harness is a dev-only tool whose
# lock cargo-fuzz regenerates; only the root Cargo.lock is a tracked artifact.
fuzz/Cargo.lock
fuzzy.log

# Profiling (samply output)
profile.json.gz
profile-*.json.gz
*-profile.json.gz
*.profile.json.gz
.loom/daemon-state.json
.loom/[0-9][0-9]-daemon-state.json
.loom/stuck-history.json
.loom/alerts.json
.loom/health-metrics.json
.loom/progress/

# Secrets — never commit
.env
.env.*

tcl-baseline-*/

# Repo Skills machine-local install metadata (absolute source path + timestamp)
.claude/skills/repo/.install-local.json
# >>> loom-managed (do not edit) >>>
# Loom runtime state (don't commit these)
.loom-in-use
.loom-checkpoint
.loom-managed
.no-changes-needed
.loom/.daemon.pid
.loom/.daemon.log
.loom/daemon.sock
.loom/daemon-loop.pid
.loom/daemon-metrics.json
.loom/loom-source-path
.loom/spawn-loop-state.json
.loom/spawn-loop.pid
.loom/stop-spawn-loop
.loom/issue-failures.json
.loom/interventions/
.loom/worktrees/
.loom/worktrees-local/
.claude/worktrees/
.loom/state.json
.loom/mcp-command.json
.loom/activity.db
.loom/claims/
.loom/locks/
.loom/signals/
.loom/status/
.loom/retry-state/
.loom/exit-codes/
.loom/sweep-checkpoint/
.loom/sweep-run/
.loom/stats/
.loom/diagnostics/
.loom/guide-docs-state.json
.loom/metrics_state.json
.loom/manifest.json
.loom/stuck-config.json
.loom/metrics/
.loom/usage-cache.json
.loom/claude-config/
.loom/tokens/
.loom/accounts.env
.loom/account-health.json
.loom/account-health.lock
.loom/CANARY
.loom/*.log
.loom/*.sock
.loom/*.tmp
.loom/*.bak
.loom/.install.lock
.loom/logs/
.loom/.resync-in-progress
# <<< loom-managed <<<

# Session handoff note (never commit — one-shot session state)
.claude/handoff.md
