# VibeSQL Storage Error Messages - Russian (ru)
# This file contains all error messages for the vibesql-storage crate.

# =============================================================================
# Table Errors
# =============================================================================

storage-table-not-found = Таблица '{ $name }' не найдена

# =============================================================================
# Column Errors
# =============================================================================

storage-column-count-mismatch = Несоответствие количества столбцов: ожидалось { $expected }, получено { $actual }
storage-column-index-out-of-bounds = Индекс столбца { $index } выходит за пределы
storage-column-not-found = Столбец '{ $column_name }' не найден в таблице '{ $table_name }'

# =============================================================================
# Index Errors
# =============================================================================

storage-index-already-exists = Индекс '{ $name }' уже существует
storage-index-not-found = Индекс '{ $name }' не найден
storage-invalid-index-column = { $message }

# =============================================================================
# Constraint Errors
# =============================================================================

storage-null-constraint-violation = Нарушение ограничения NOT NULL: столбец '{ $column }' не может быть NULL
storage-unique-constraint-violation = { $message }

# =============================================================================
# Type Errors
# =============================================================================

storage-type-mismatch = Несоответствие типа в столбце '{ $column }': ожидалось { $expected }, получено { $actual }

# =============================================================================
# Transaction and Catalog Errors
# =============================================================================

storage-catalog-error = Ошибка каталога: { $message }
storage-transaction-error = Ошибка транзакции: { $message }
storage-row-not-found = Строка не найдена

# =============================================================================
# I/O and Page Errors
# =============================================================================

storage-io-error = Ошибка ввода/вывода: { $message }
storage-invalid-page-size = Недопустимый размер страницы: ожидалось { $expected }, получено { $actual }
storage-invalid-page-id = Недопустимый идентификатор страницы: { $page_id }
storage-lock-error = Ошибка блокировки: { $message }

# =============================================================================
# Memory Errors
# =============================================================================

storage-memory-budget-exceeded = Превышен лимит памяти: используется { $used } байт, лимит { $budget } байт
storage-no-index-to-evict = Нет индекса для вытеснения (все индексы уже на диске)

# =============================================================================
# General Errors
# =============================================================================

storage-not-implemented = Не реализовано: { $message }
storage-other = { $message }
