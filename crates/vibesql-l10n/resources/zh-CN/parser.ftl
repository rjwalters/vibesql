# VibeSQL 解析器/词法分析器本地化 - 简体中文
# 此文件包含词法分析器和解析器错误的所有用户可见字符串。

# =============================================================================
# 词法分析器错误头
# =============================================================================

lexer-error-at-position = 词法分析错误（位置 { $position }）：{ $message }

# =============================================================================
# 字符串字面量错误
# =============================================================================

lexer-unterminated-string = 未终止的字符串字面量

# =============================================================================
# 标识符错误
# =============================================================================

lexer-unterminated-delimited-identifier = 未终止的分隔标识符
lexer-empty-delimited-identifier = 不允许空的分隔标识符

# =============================================================================
# 数字字面量错误
# =============================================================================

lexer-invalid-scientific-notation = 无效的科学记数法：'E' 后应有数字

# =============================================================================
# 占位符错误
# =============================================================================

lexer-expected-digit-after-dollar = '$' 后应有数字（编号占位符）
lexer-invalid-numbered-placeholder = 无效的编号占位符：${ $placeholder }
lexer-numbered-placeholder-zero = 编号占位符必须是 $1 或更大（不能使用 $0）
lexer-expected-identifier-after-colon = ':' 后应有标识符（命名占位符）

# =============================================================================
# 变量错误
# =============================================================================

lexer-expected-variable-after-at-at = @@ 后应有变量名
lexer-expected-variable-after-at = @ 后应有变量名

# =============================================================================
# 运算符错误
# =============================================================================

lexer-unexpected-pipe = 意外的字符：'|'（您是否想输入 '||'？）

# =============================================================================
# 一般错误
# =============================================================================

lexer-unexpected-character = 意外的字符：'{ $character }'
